#include "ezui/style.h"

// Style and Theme are header-only for now.
// This translation unit exists so the build system has something to compile
// and we can add non-inline helpers later without breaking ABI.

namespace EzUI {

} // namespace EzUI
